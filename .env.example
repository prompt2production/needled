# Application
APP_NAME=myapp

# Database Configuration (for Docker Compose)
DB_USER=appuser
DB_PASSWORD=change_this_password
DB_NAME=appdb
DB_PORT=5466

# Prisma Database URL (update to match above values)
DATABASE_URL="postgresql://appuser:change_this_password@localhost:5466/appdb"
